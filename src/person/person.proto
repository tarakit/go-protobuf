syntax = "proto3";
package simple.protobuf;

/* This is the message for Person 
    to interchange data on http
*/
message Person {
    string name = 1;
    int32 age = 2;
    string gender = 3;

    // This will take the list of Phone Number
    repeated string phoneNumber = 4; 
}